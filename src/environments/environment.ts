// Entorno de DESARROLLO (se usa con `ng serve`).
// Para producción se reemplaza por environment.prod.ts (ver angular.json).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api/v1',
};
